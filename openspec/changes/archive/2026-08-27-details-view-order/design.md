# Design

Reorder the existing JSX blocks in `ItemDrawer` — no logic changes. The
external-manager note stays directly under the title; the metadata block
(created/updated/creator) stays just above Activity.
