## 1. Implementation

- [x] 1.1 `duplicateBoard` service method + route + client; service tests for the three spec scenarios
- [x] 1.2 `DuplicateBoardDialog` (name + copy columns/share checkboxes) wired into the more menu with navigation to the copy; UI smoke

## 2. Verification

- [x] 2.1 tsc, lint, tests green; Playwright smoke duplicating a board with columns
