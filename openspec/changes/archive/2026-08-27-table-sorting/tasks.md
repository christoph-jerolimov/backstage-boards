## 1. Implementation

- [x] 1.1 `sortItems` helper with unit tests; sortable headers in `TableView` (sortDescriptor state shared across grouped tables)

## 2. Verification

- [x] 2.1 tsc, lint, tests green; Playwright smoke of ascending/descending title sort
