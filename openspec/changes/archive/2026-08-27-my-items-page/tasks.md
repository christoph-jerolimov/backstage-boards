# Tasks

## 1. Backend

- [x] 1.1 `MyBoardItem` type; `listMyItems` service method with access
      filtering; `GET /my-items` route; service tests

## 2. Frontend

- [x] 2.1 `boardsApi.listMyItems`; `MyItemsPage` grouped by board with
      status/due/tags and item links; route + list-page link

## 3. Verification

- [x] 3.1 `yarn tsc`, unit tests, lint, Playwright smoke
