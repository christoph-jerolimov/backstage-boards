## 1. Implementation

- [x] 1.1 Add `plugins/boards/e2e-tests/board-drag.test.ts` (seeded board, cross-column drag with reload persistence, drop-on-card insert-before, menu fallback move) and verify `yarn test:e2e` passes against the dev servers

## 2. Verification

- [x] 2.1 tsc + lint green; full e2e run recorded in the change notes
