## 1. Backend

- [x] 1.1 Migration + archived gate in `requireBoard`, archive-on-delete, `hardDeleteBoard`, listing exclusion, purge extension; service tests for the six spec scenarios
- [x] 1.2 Routes (`delete-now`), action description update, API client methods

## 2. Frontend

- [x] 2.1 Archival alert with deletion date + admin delete-now confirm, read-only page state, "Archive board…" menu wording and dialog text; UI smoke of archive → link-only read-only → delete now

## 3. Verification

- [x] 3.1 tsc, lint, tests green; Playwright smoke of the archived board experience
