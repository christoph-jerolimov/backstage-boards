## 1. Backend

- [x] 1.1 Implement `duplicateItem` (fields, unchecked checklist,
      description version, position after source, WIP guard, creation
      record) with the route; verify with service tests for the spec
      scenarios.

## 2. Frontend

- [x] 2.1 API method and item menu "Duplicate" entry (writers only);
      verify with a menu-surface test that the API is called and the
      entry hides for readers.

## 3. Docs

- [x] 3.1 Update the item menu documentation and README; verify
      wording.
