## 1. Implementation

- [x] 1.1 Add the `entityRef` filter to `listBoards`, the route, the client, and a `useBoardsByEntityQuery` hook used by `EntityBoardsContent`; unit test filtered listing incl. access enforcement

## 2. Verification

- [x] 2.1 tsc, lint, tests green; live API check of `?entityRef=`
