## 1. Implementation

- [x] 1.1 Register breadcrumb entries in `BoardPage` and remove the custom back button; verify the plugin header shows "Boards / <name>" and the "Boards" crumb navigates to the list
