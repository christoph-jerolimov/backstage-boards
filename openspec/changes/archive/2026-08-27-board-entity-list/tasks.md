# Tasks

## 1. Backend

- [x] 1.1 Migration, types, service read/write/filter, router, actions,
      service tests (multi-ref CRUD, filter matching, migration of the
      legacy column)

## 2. Frontend

- [x] 2.1 EntityPicker + BoardSettingsDialog, menu entry, header links,
      board list page rendering, API client

## 3. Verification

- [x] 3.1 tsc, tests, lint, Playwright smoke (add two entities via the
      dialog, remove one, entity tab filtering still works)
