## 1. Backend

- [x] 1.1 Implement `moveItemToBoard` (transactional copy/re-point,
      priority-by-name, WIP guard, move record, archive original,
      signals for both boards) with the route; verify with service
      tests covering the spec scenarios.

## 2. Frontend

- [x] 2.1 API method, `MoveItemDialog` (board search → column select →
      Move), and the item menu entry (writers, non-external); verify
      with dialog tests for the two-step flow and the API call.

## 3. Docs

- [x] 3.1 Update `docs/features/items.md` and README; verify wording.
