# Tasks

## 1. Implementation

- [x] 1.1 Parse `schedule` with the framework helper, spread into
      scheduleTask, update config schema + sample + tests; verify dev
      backend starts with a minutes-based schedule
