## 1. Menu entry

- [x] 1.1 Add the "Copy link" entry to `ItemMenu` (absolute URL,
      clipboard write with fallback, "Link copied" feedback, offered
      to readers); verify with ItemMenu-surface tests asserting the
      copied URL and reader availability.

## 2. Docs

- [x] 2.1 Mention permalinks in `docs/features/items.md` and the
      README; verify wording.
