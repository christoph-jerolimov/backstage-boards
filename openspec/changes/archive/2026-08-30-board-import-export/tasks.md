## 1. Backend

- [x] 1.1 Implement `exportBoard` (JSON + CSV) with the read-gated
      route; verify with service/router tests including CSV escaping.
- [x] 1.2 Implement `importBoard` (validate first, then create board,
      columns' colors/limits, priorities, description, items) with the
      route; verify with round-trip and rejection tests.

## 2. Frontend

- [x] 2.1 API methods, Export dialog in the board menu (JSON and CSV
      downloads), Import dialog on the board list creating and opening
      the board; verify with dialog tests.

## 3. Docs

- [x] 3.1 Add `docs/features/import-export.md` (including the JSON
      format for external converters), mkdocs nav, README; verify
      wording.
