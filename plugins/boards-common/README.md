# @internal/plugin-boards-common

Shared types and helpers for the boards plugin: board, column, item, comment,
change, and permission types, `text:` ref helpers, and permission level
utilities. Used by `@internal/plugin-boards` (frontend),
`@internal/plugin-boards-backend`, and future sync modules.
